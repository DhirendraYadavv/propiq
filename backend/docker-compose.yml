services:
  postgres:
    image: postgres:15
    container_name: propiq-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: propiq
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: